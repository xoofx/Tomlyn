---
title: Trivia and roundtrip
---

For full-fidelity round-tripping (preserving comments and formatting), use the syntax APIs (`SyntaxParser` / `DocumentSyntax`) and enable trivia capture where applicable.
