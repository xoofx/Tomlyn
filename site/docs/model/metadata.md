---
title: Metadata store
---

When `TomlSerializerOptions.MetadataStore` is configured, Tomlyn can capture and attach metadata to objects and DOM nodes, such as:

- Source spans
- Display preferences
- Trivia (comments/whitespace) when enabled
