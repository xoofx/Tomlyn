---
title: TomlTable / TomlArray
---

The DOM types represent TOML values:

- `TomlTable` for tables (key/value mappings)
- `TomlArray` for arrays
- `TomlTableArray` for arrays of tables

You can serialize/deserialize DOM instances using `TomlSerializer` and `TomlWriter` helpers.
