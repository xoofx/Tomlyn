---
title: Docs
---

This section will host the Tomlyn v1 documentation (new API, TOML 1.1.0 support, source generation, NativeAOT guidance).

For now:
- Source: `TomlLexer`, `TomlParser`, `SyntaxParser` in the `Tomlyn.Parsing` namespace.
- High-level: `TomlSerializer`, `TomlSerializerOptions`, and `TomlSerializerContext`.

