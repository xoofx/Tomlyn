---
title: Supported types
---

Tomlyn supports common .NET primitives and framework types (numbers, strings, `DateTimeOffset`, `Guid`, `Uri`, `TimeSpan`, enums, etc.), and collection shapes (arrays, lists, sets, dictionaries, immutable collections).

For a complete list, see the API docs for built-in converters and `TomlSerializerOptions.Converters`.
